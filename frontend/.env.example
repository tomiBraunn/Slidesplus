# Frontend (Vite) environment variables — copy to .env.local and fill in.
# Only VITE_* vars are exposed to the browser. Never put server secrets here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_API_URL=
VITE_GA_ID=
